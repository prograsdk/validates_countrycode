RSpec.describe ValidatesCountrycode do
  it "has a version number" do
    expect(ValidatesCountrycode::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
